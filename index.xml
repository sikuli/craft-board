<craft>
    <stack>
        <row>
            <content></content>
        </row>
        <script type="text/craftml">
            function main(params, scope){                
                var row = scope.solids[0]
                var xsize = row.layout.size.x
                var ysize = row.layout.size.y
                return '<cube xsize="' + xsize + '" ysize="' + ysize + '" zsize="2"></cube>'
            }
        </script>
    </stack>
</craft>